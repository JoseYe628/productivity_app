
import 'package:flutter/material.dart';

class ReordenableTodoItem extends StatelessWidget {
  const ReordenableTodoItem({super.key});

  @override
  Widget build(BuildContext context){
    return Container(
      child: const Text("ReordenableTodoItem widget"),
    );
  }
}
